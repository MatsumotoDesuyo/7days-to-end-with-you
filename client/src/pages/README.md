# Name

common

# Note
(etc)
  L Route.tsx(ルーティングを記述する)
  L pages(各ページごとに分ける)
      L 各種ページ名
          L index.tsx
          L component1.tsx(このページでしか使用しないコンポーネント)
          L index.css(そもそもcss、scssは使用するのか？)
          L index.test.ts
          L `__snapshots__`
